name: SQLite Tools
version: 0.0.1
schema: v1
mcpServers:
  - name: sqlite-server
    type: stdio
    command: npx
    args:
      - mcp-sqlite
      - /tmp/test.db
    env:
      DB_PATH: /tmp/test.db
