name: Server with secrets
version: 0.0.1
schema: v1
mcpServers:
  - name: secret-server
    command: npx
    args:
      - "@some/mcp-server"
    env:
      API_KEY: ${{ secrets.MY_API_KEY }}
      DB_URL: ${{ secrets.DATABASE_URL }}
